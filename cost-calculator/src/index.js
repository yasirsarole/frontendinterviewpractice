import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// API for products
// Google - Dummy products api
// Display all the products
// Add - Add to cart and Remove from cart functionality
// Display cart items
// Display cart value/totalamount in dollars
// Add an ability to increase the cart item quantity
